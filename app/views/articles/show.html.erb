<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @article.name %>
</p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @article.content %>
</p>
<p>
  <strong>Image for Content</strong><br>
<%# <%= image_tag(@article.cover.url) if @article.cover %>
<image src="<%=url_for(@article.cover) %>"
</p>
<h2>Comments</h2>
<p>
  <% if @article.comments.count >1 %>
  <%= @article.comments.count %> users commented on this post.
<% else %>
     <%= @article.comments.count %> user commented on this post.
<% end %>
</p>
<%= render @article.comments%>





<h2>Add a comment:</h2>
<%= render "comments/form" %>


<br />

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
